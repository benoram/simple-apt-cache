# apt-cacher-ng security configuration

# Allow access to the admin interface from any IP
# In production, you may want to restrict this
AdminAuth: admin:changeme

# Allow all clients to use the cache
# Restrict to specific networks if needed by uncommenting and modifying:
# AllowUserPorts: 0

# Enable this to allow CONNECT method for HTTPS tunneling
# AllowedConnectPorts: 443
